# 01 — 架构决策记录

本文件是轻量决策台账。正式 ADR 可在总纲批准后放入 `docs/adr/`。

## 已确认

| ID | 决策 | 日期 | 说明 |
|---|---|---|---|
| D-001 | UI 使用 Qt Quick/QML | 2026-08-30 | 保证现代交互与动画能力。 |
| D-002 | Windows 11 优先 | 2026-08-30 | 首先在本地 Windows 11 环境开发与验收。 |
| D-003 | 支持范围限定为 Windows 10/11 | 2026-08-30 | 暂不承担 Linux、macOS 的首发兼容成本。 |
| D-004 | 旧 JS 音源插件要求 100% 兼容 | 2026-08-30 | 必须由契约测试和真实脚本证明。 |
| D-005 | 可抛弃旧内部实现 | 2026-08-30 | 不以逐文件翻译 Electron 代码为迁移策略。 |
| D-006 | 网易云 API 和 AMLL 实现可替换 | 2026-08-30 | 保留产品能力，不绑定原依赖。 |
| D-007 | 设计先行、AI 自动化实施 | 2026-08-30 | 总纲批准前不进行批量实现。 |
| D-008 | 播放器按 B → C 分阶段交付，必要时以 A 作为短期落地层 | 2026-08-30 | 首个目标是 Gapless、Crossfade、ReplayGain、EQ 和高解析；只有相关项目难以接入且能明显缩短首个可用版本时才暂降 A，成功后恢复 B，最终升级 C。 |
| D-009 | Figma 是视觉设计真源，优先采用官方 Figma to Qt | 2026-08-30 | 导出 QML 作为可再生成的表现层资产，手写包装层负责交互、状态、模型与业务绑定。 |
| D-010 | 主后端采用 C++20 + Qt | 2026-08-30 | 原生连接 QML、Qt 模型、Windows 与音频生态；Rust 只允许作为有明确复用收益的窄接口模块。取消 Qt Bridges for Rust PoC。 |
| D-011 | 首个可运行 MVP 聚焦旧 JS 音源兼容与基本播放闭环 | 2026-08-30 | 先跑通启动、在线/本地歌曲获取、播放队列、播放控制、进度、音量、基础歌词和错误恢复，再按既定 A/B/C 路线扩展播放器。 |
| D-012 | 不迁移旧应用数据 | 2026-08-30 | 不导入旧设置、历史、缓存、数据库或插件配置；旧代码只作参考，本地音乐由新库重新扫描，原音频文件保持原位。 |
| D-013 | 网易云首版支持登录、每日推荐和我的歌单 | 2026-08-30 | 账户能力使用专用服务；不能假设旧自定义音源插件提供歌单接口。 |
| D-014 | 常见本地音频格式按测试矩阵验收 | 2026-08-30 | 至少覆盖 MP3、FLAC、M4A、AAC、OGG/OGA、Opus、WAV、APE、WV、AIFF/AIF、TTA、WMA；MP4 音频容器列入兼容验证。扩展名可见不等于解码、Seek、元数据和歌词全链路通过。 |
| D-015 | UI 采用 Apple 风格方向，最终以 Figma 确认 | 2026-08-30 | 当前只确定方向，不凭口头描述提前锁定组件尺寸、动效和视觉令牌。 |
| D-016 | 正常使用内存目标低于 400 MiB | 2026-08-30 | 在 Windows 11、i9-13980 + RTX 4060 笔记本测量整个进程树；具体基准场景与峰值/稳态口径仍需补齐。 |
| D-017 | 使用 GitHub Releases 发布，暂不做自动更新 | 2026-08-30 | 首发工件至少应包含安装版；便携 ZIP 为推荐的诊断与备用工件，待用户确认。 |
| D-018 | 旧 JS 音源生态兼容优先于重新设计插件 API | 2026-08-30 | 参考旧版正常行为，同时增加进程隔离、超时、取消、输入输出验证和可恢复崩溃，不要求复刻其内部 Chromium 实现。 |
| D-019 | 在线服务不迁移旧内部实现，以性能、效果和可靠性选型 | 2026-08-30 | 排行榜、搜索和歌单统一进入可替换的 `IOnlineProvider`；网易云优先验证 C++/Qt 原生实现，旧 Node API 只作正确性基准和必要回退。 |

## 候选，尚未确认

| ID | 候选方案 | 需要的证据 |
|---|---|---|
| C-002 | 播放器第一版使用 Qt Multimedia/FFmpeg | 在 Windows 11 验证网络流、无缝切歌、设备切换、格式、进度精度和内存。 |
| C-003 | 后续播放器参考 Qcm/fooyin，采用 FFmpeg + cubeb | 当 Qt Multimedia 不满足功能或性能门槛时再切换。 |
| C-004 | 旧音源使用按需启动的 Node/V8 SourceHost | 用真实音源脚本验证兼容率、启动时间、常驻内存和崩溃隔离。 |
| C-005 | 后续增加 QuickJS 运行时并保留 Node 回退 | 验证 Windows/MSVC 支持和全部 `globalThis.lx` API。 |
| C-006 | 网易云优先采用 C++/Qt 原生适配器 | 比较 Qcm/ncrequest、QCloudMusicApi、当前 Node API 与独立 ncm-api-rs 的 Windows 可用性、账号功能、内存、延迟和维护成本；Rust 方案只允许作为窄服务候选。 |
| C-007 | Apple Music 风格歌词原生 QML/QSG 重建 | 建立 60 FPS 原型并测试逐字高亮、模糊、弹簧、流体背景和显存。 |
| C-009 | CPack + Inno Setup 生成安装 EXE，同时生成便携 ZIP | 用干净 Windows 11/10 环境验证安装、覆盖升级、卸载、快捷方式、文件关联、非管理员安装、便携数据目录和 GitHub Release 上传。 |

## 已否决

| ID | 方案 | 原因 |
|---|---|---|
| R-001 | 主界面继续使用 Electron/Chromium | 与降低基础内存和进程开销的目标冲突。 |
| R-002 | 用 QWebEngine 单独承载 AMLL 作为最终方案 | 为单一视图重新引入 Chromium，除非 PoC 证明原生实现不可接受，否则不采用。 |
| R-003 | Rust-first + Qt Bridges for Rust 作为主后端 | 当前项目没有足以抵消 Beta 桥接、双工具链和跨语言调试成本的明确收益。 |
