# ListenFree Desktop：Codex 工作入口

本仓库用于将 LX Music Desktop 的能力重新设计为面向 Windows 11、兼容 Windows 10 的 Qt Quick/QML 桌面音乐应用。

## 每次开始工作前必须执行

1. 完整阅读 `docs/design/README.md`。
2. 按索引顺序阅读其中标记为“必读”的设计文档。
3. 检查 `docs/design/01-decisions.md`：只有状态为“已确认”的条目才是约束。
4. 检查 `docs/design/02-questionnaire.md`：不得擅自替用户回答尚未决定且会改变架构的问题。
5. 只有 `docs/design/99-master-plan.md` 状态变为“已批准”后，才可开始批量搭建和迁移。

## 长期约束

- Windows 11 是首要开发和验收平台；Windows 10 是后续兼容平台。
- UI 使用 Qt Quick/QML，并保留流畅动画能力。
- 降低内存、启动时间和后台开销是核心目标，性能结论必须由可复现基准支持。
- 旧版 LX Music 自定义 JS 音源插件必须 100% 兼容；兼容性必须通过参考脚本契约测试证明。
- UI 不直接访问数据库、网络实现、音频解码器或插件运行时。
- 音频 PCM 等高频大数据不得经过 QML 或通用 JSON IPC。
- 内存和资源安全是硬约束：优先使用 RAII、Qt parent ownership、智能指针和明确的线程生命周期；禁止拥有型裸指针、泄露 QObject、未取消的后台任务、无界缓存/队列和跨线程悬空引用。
- 每个里程碑都要检查资源释放、重复启动/退出、取消、异常路径和进程树内存；发现泄露或持续增长必须先修复再推进。
- 优先复用成熟开源组件（Qt 官方模块、SQLite 及经过审计的活跃项目），不要重复造基础设施；正式引入前记录来源、固定版本、维护状态、修改范围、许可证、包体和常驻内存影响，并保留可替换边界。
- 编写新模块前必须先检索是否已有成熟、活跃且可验证的开源实现；存在合适实现时优先直接集成、裁剪或移植其代码，不重复造轮子。复用代码仍需保留上游来源、固定提交、改动范围和本地验证记录，并接入现有窄接口与生命周期测试。
- Figma to Qt 只负责可再生成的表现层资产，默认放入 `ui/imported/figma/`；业务逻辑、状态和接口绑定写在 `ui/components/`、`ui/pages/` 或 C++ 包装层，不修改生成文件。
- 不得直接整包复制旧 Electron 项目；按批准后的服务边界迁移可验证的行为和数据。
- 架构变化应先更新设计文档和决策记录，再实施代码。

## 子 Agent 协作

- 对可独立并行的代码调查、实现、测试或审查任务，主动调用子 Agent 缩短反馈周期；主 Agent 保留架构决策、集成、完整构建和最终验收责任。
- 派发前明确每个子 Agent 的目标、文件或模块所有权、完成标准和禁止修改范围；并行任务不得拥有重叠文件。
- 告知子 Agent 当前有其他协作者，不回退或覆盖他人改动；遇到共享边界变化时先报告，由主 Agent 统一集成。
- 子 Agent 必须报告修改文件、验证命令、失败项和资源生命周期风险；主 Agent 在接收结果后检查差异，并统一运行 Debug/Release、CTest 与必要的内存/进程测试。
- 涉及 QObject、线程、定时器、缓存、数据库连接或子进程时，任务完成标准必须包含停止、取消、异常和析构路径；所有权不清晰或可能泄露时不得合入。

## 给后续 Codex 的启动提示

> 你正在维护 ListenFree Desktop。请先完整阅读 `AGENTS.md` 与 `docs/design/README.md` 指定的全部必读文档，汇总已确认决策、未决问题和当前阶段，然后只执行总纲允许的工作。实现时优先采用成熟开源组件，持续检查内存、句柄、线程和进程生命周期，确保退出后无泄露、无悬空任务、无无界增长。

